const numbers = ['one', 'two', 'three', 'four']
const colors = ['red', 'green', 'blue']

// // Length method
// console.log(numbers.length)

// push method
// numbers.push("five");
// console.log(numbers)

//pop method removes the last item of the array
// const lastnum = numbers.pop();
// console.log(numbers)
// console.log(lastnum)
// also returns the deleted item

//indexOf method provides you the index of the array;
// console.log(numbers.indexOf('three'))

// shift() method removes the first item from array;
// colors.shift()
// console.log(colors)
// const shiftedElements = colors.shift()
// console.log(shiftedElements)

//unshift method include the item at the begening of the array.
// colors.unshift('orange')
// console.log(colors)

//toString() method returns onestring after adding up all the items from array.
// console.log(colors.toString());
// console.log(colors.join(' '))

//concatinate arrays

// console.log(colors+numbers) note: incorrect method to concatinate
// const data = numbers.concat(colors)
// console.log(data)
